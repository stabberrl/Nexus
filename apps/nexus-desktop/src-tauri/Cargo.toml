[package]
name = "nexus-desktop"
version = "1.0.0"
description = "Nexus AI - Asistente de IA personal"
authors = ["Nexus"]
edition = "2021"

[package.metadata]

[lib]
name = "nexus_desktop_lib"
crate-type = ["lib", "cdylib", "staticlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["devtools"] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "3", features = [] }
